/** Centered spinner with an optional status line. */
export declare function Loading({ label, compact, className, }: {
    label?: string;
    /** Tight variant for a container that cannot give up 2.5rem of padding. */
    compact?: boolean;
    className?: string;
}): import("react").JSX.Element;
