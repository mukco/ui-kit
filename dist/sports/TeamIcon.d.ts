interface Props {
    teamId: string | number;
    /** Rendered size in px. */
    size?: number;
}
/** A team's crest from the app-configured logoUrl. */
export declare function TeamIcon({ teamId, size }: Props): import("react").JSX.Element;
export {};
