/** Educational panel explaining models, parameters and metrics — shown from
    ML builders' "how does this work" affordances. */
export declare function NNExplainer({ onClose }: {
    onClose?: () => void;
}): import("react").JSX.Element;
